<UserControl x:Class="PS5_Controller_Tools.SpeakerWaveVisualizerOverlay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PS5_Controller_Tools"
             Width="55"
             Height="12"
             IsHitTestVisible="False"
             SnapsToDevicePixels="True">

    <Grid>
        <Border x:Name="GlowHost"
                Background="Transparent"
                CornerRadius="10"
                Opacity="0.95">
            <Border.Effect>
                <BlurEffect Radius="8"/>
            </Border.Effect>
        </Border>

        <Canvas x:Name="BarsCanvas"
                Width="55"
                Height="12"
                Background="Transparent"/>
    </Grid>
</UserControl>